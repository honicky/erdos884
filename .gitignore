# Lean / Lake build artifacts (present once this becomes a Lake project)
/.lake/
/lake-packages/
/build/
*.olean
*.ilean
*.trace

# Editor / OS
.DS_Store
*.swp

# verify.sh scratch
/.axle-result.json

# Lake logs (lake-manifest.json IS committed, for reproducibility)
/lake-update.log
/lake-build.log
